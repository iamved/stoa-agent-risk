# Environment values. Stoa reads terraform.tfvars and *.auto.tfvars so the
# resolved project below is what the agents are attributed to — not the
# "marlowe-dev" default in variables.tf.
project_id = "marlowe-prod"
